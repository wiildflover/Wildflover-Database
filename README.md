# Wildflover-Database

Skins Database — League of Legends skin mod files (`.zip`) used by the Wildflover app.

Structure: `skins/<championId>/<skinId>/<skinId>.zip` (variants under `skins/<championId>/<skinId>/<variantId>/<variantId>.zip`).
